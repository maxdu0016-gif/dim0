x-qdrant: &qdrant-base
  image: qdrant/qdrant:latest

x-postgres: &postgres-base
  image: postgres:15
  environment:
    POSTGRES_DB: topix
    POSTGRES_USER: topix
    POSTGRES_HOST_AUTH_METHOD: trust

x-redis: &redis-base
  image: redis:7-alpine
  command: redis-server --appendonly yes

services:
  qdrant:
    <<: *qdrant-base
    container_name: qdrant
    ports:
      - "${QDRANT_HOST_PORT:-6333}:6333"
    volumes:
      - qdrant_data:/qdrant/storage

  postgres:
    <<: *postgres-base
    container_name: postgres
    ports:
      - "${POSTGRES_HOST_PORT:-5432}:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
      - ./schema.sql:/docker-entrypoint-initdb.d/schema.sql
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "topix"]
      interval: 5s
      timeout: 2s
      retries: 10

  redis:
    <<: *redis-base
    container_name: redis
    ports:
      - "${REDIS_HOST_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 2s
      retries: 10

  backend:
    image: winlp4ever/dim0-backend:${DIM0_VERSION:?DIM0_VERSION is required}
    container_name: topix-backend
    volumes:
      - ../${ENVFILE:-.env}:/.env:ro
    environment:
      DOPPLER_TOKEN: ${DOPPLER_TOKEN}
      API_HOST: 0.0.0.0
      API_PORT: ${API_PORT:-8081}
      QDRANT_HOST: ${QDRANT_HOST:-qdrant}
      QDRANT_PORT: ${QDRANT_PORT:-6333}
      POSTGRES_HOST: ${POSTGRES_HOST:-postgres}
      POSTGRES_PORT: ${POSTGRES_PORT:-5432}
      REDIS_HOST: ${REDIS_HOST:-redis}
      REDIS_PORT: ${REDIS_PORT:-6379}
    command: ["python", "-m", "topix.api.app", "--stage", "prod"]
    depends_on:
      - postgres
      - qdrant
      - redis
    ports:
      - "${API_PORT:-8081}:${API_PORT:-8081}"

  webui:
    image: winlp4ever/dim0-webui:${DIM0_VERSION:?DIM0_VERSION is required}
    container_name: topix-webui
    environment:
      API_ORIGIN: ${API_ORIGIN:-http://localhost:${API_PORT:-8081}}
      VITE_BILLING_ENABLED: ${VITE_BILLING_ENABLED:-false}
    depends_on:
      - backend
    ports:
      - "${APP_PORT:-3000}:80"

volumes:
  qdrant_data:
  pg_data:
  redis_data:
