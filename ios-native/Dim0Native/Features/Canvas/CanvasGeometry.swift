import CoreGraphics

enum CanvasGeometry {
    static let contentSize = CGSize(width: 12_000, height: 12_000)
    static let syncOrigin = CGPoint(x: contentSize.width / 2, y: contentSize.height / 2)
}

